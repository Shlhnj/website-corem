source "https://rubygems.org"

# This matches the Jekyll + plugin versions GitHub Pages runs in production,
# so `bundle exec jekyll serve` locally behaves the same as the deployed site.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer bundles these by default; github-pages needs them.
gem "webrick"
